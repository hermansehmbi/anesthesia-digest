feedparser>=6.0.0
openpyxl>=3.1.0
edge-tts>=6.1.0
httpx>=0.27.0
pydub>=0.25.1
# pydub needs ffmpeg on the system PATH to read/write/mix MP3s.
#   GitHub Actions (ubuntu-latest): ffmpeg is preinstalled — nothing to do.
#   Local Mac: install it with `brew install ffmpeg`.
